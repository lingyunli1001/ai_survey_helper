# ai_survey_helper

Testing how well LLM-generated survey respondents reproduce real human survey data.

Conditions language models on demographic profiles, collects their survey responses,
and compares the results against published human benchmarks to measure where
synthetic respondents diverge from real ones.
